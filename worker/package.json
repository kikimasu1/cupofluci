{
  "name": "cupofluci-guestbook",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute cupofluci-guestbook --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute cupofluci-guestbook --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.110.0"
  }
}
